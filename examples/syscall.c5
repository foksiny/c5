// no standard library needed, because we are already using the syscall to do a stdout operation.
void main() {
    syscall(3, 1, 1, "Hello, world!\n", 14); // args: syscall_type: 3, rax: 1, rdi: 1, rsi: "Hello, world!\n", rdx: 14
}