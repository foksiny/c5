include <std.c5h>
include <math.c5h>

void main() {
    math::Vec2 v1;
    v1.x = 10;
    v1.y = 20;

    int<32> r = math::add(v1.x, v1.y);
    std::printf("Result: %d\n", r);

    std::printf("PI_INT: %d\n", math::PI_INT);

    int<32> op = math::Operation::ADD;
    std::printf("OP ADD: %d\n", op);
}